<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.abb.bye.mapper.SiteConfigMapper">

    <sql id="columns">
        gmt_create,
        gmt_modified,
        id,
        site,
        config_key,
        content,
        env,
        status,
        domains,
        name,
        platform
    </sql>

    <select id="list" resultType="com.abb.bye.client.domain.SiteConfigsDO" parameterType="int">
        select
        <include refid="columns"/>
        from site_configs
        where site=#{site}
        order by gmt_modified desc
    </select>

    <select id="get" resultType="com.abb.bye.client.domain.SiteConfigsDO" parameterType="long">
        select
        <include refid="columns"/>
        from site_configs
        where id=#{id}
    </select>

    <insert id="insert" parameterType="com.abb.bye.client.domain.SiteConfigsDO">
        insert site_configs
        ( id, gmt_create, gmt_modified, site, config_key, content, env, status, domains, name, platform)
        values
        ( #{id}, now() ,now(), #{site}, #{configKey}, #{content}, #{env}, #{status}, #{domains}, #{name} ,#{platform})
    </insert>


    <update id="update" parameterType="com.abb.bye.client.domain.SiteConfigsDO">
        update site_configs
        set gmt_modified=now()
        ,content=#{content}
        ,domains=#{domains}
        ,status=#{status}
        ,name=#{name}
        ,config_key=#{configKey}
        ,env=#{env}
        ,platform=#{platform}
        where id=#{id}
    </update>

</mapper>