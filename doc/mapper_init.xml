<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="init">

    <update id="createItemTable">
        CREATE TABLE IF NOT EXISTS item(
            id integer PRIMARY KEY AUTOINCREMENT,
            user_id long NOT NULL,
            gmt_create datatime NOT NULL,
            gmt_modified datatime NOT NULL,
            title varchar(128),
            pics varchar(128),
            priority int default 100,
            status tinyint default 1,
            categories varchar(32),
            keywords varchar(32),
            attributes varchar(128),
            summary varchar(256)
        );
    </update>

    <update id="createEntityTable">
        CREATE TABLE IF NOT EXISTS entity(
        id integer PRIMARY KEY AUTOINCREMENT,
        item_id long NOT NULL,
        gmt_create datatime NOT NULL,
        gmt_modified datatime NOT NULL,
        priority int default 100,
        status tinyint default 1,
        attributes varchar(32),
        content varchar(256),
        );
    </update>

</mapper>